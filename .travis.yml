sudo: required

services:
  - docker

before_install:
  - deployment/build-server.sh .
  - deployment/run-server.sh
  - docker ps -a

script:
  - docker exec server-container pytest --cov-report term-missing --cov

after_success:
  - docker exec server-container coveralls

after_script:
  - docker rm -f server-container